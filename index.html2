<!DOCTYPE html>
<html>
<head>
    <title>Happy Birthday ❤️</title>

    <style>
        body {
            background-color: #fff0b3;
            font-family: Arial;
            text-align: center;
            padding-top: 100px;
        }

        .box {
            background: white;
            width: 60%;
            margin: auto;
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0px 0px 10px gray;
        }

        button {
            padding: 15px 25px;
            font-size: 18px;
            margin: 10px;
            border: none;
            border-radius: 10px;
            cursor: pointer;
        }

        #yes {
            background-color: lightgreen;
        }

        #no {
            background-color: lightcoral;
            position: absolute;
        }

        h1 {
            color: #ff4081;
        }
    </style>
</head>

<body>

<div class="box">
    <h1>Happy Birthday 🎂</h1>

    <h2>Do you want a big birthday surprise? 🎁</h2>

    <button id="yes" onclick="yesClick()">YES 😍</button>
    <button id="no" onmouseover="moveNo()">NO 😜</button>

    <p id="message"></p>
</div>

<script>
    function yesClick() {
        document.getElementById("message").innerHTML =
        "Yayyy! 🎉🎂 You are the most special person in my life! May all your dreams come true. Happy Birthday! ❤️";
    }

    function moveNo() {
        var x = Math.random() * window.innerWidth;
        var y = Math.random() * window.innerHeight;

        document.getElementById("no").style.left = x + "px";
        document.getElementById("no").style.top = y + "px";
    }
</script>

</body>
</html>
